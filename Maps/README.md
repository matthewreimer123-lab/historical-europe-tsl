# WorldBuilder map placeholder

`HistoricalEurope.lua` is generated from geographic source data by
`scripts/generate_map.py`. It is the selectable Civ V map script.

WorldBuilder is optional and is used only for inspection or touch-up exports.
